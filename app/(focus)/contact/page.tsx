import { ContactPageContent } from '@/components/contact/QuoteFlow'

export const metadata = { title: 'Contact — Promotion Efficiency' }

export default function ContactPage() {
  return <ContactPageContent />
}
